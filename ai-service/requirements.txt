# AI Service Dependencies

# FastAPI và ASGI server
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# Pydantic settings (compatible versions)
pydantic==2.10.0
pydantic-settings==2.7.0

# Qdrant vector database client
qdrant-client==1.7.3

# Neo4j graph database client (for GraphRAG)
neo4j==5.15.0

# Cohere API client (for embedding only)
cohere>=5.0.0

# Groq API (direct SDK)
groq>=0.11.0

# LangChain Community (ONLY for document loaders/text splitters)
# Let pip resolve compatible versions together
langchain-text-splitters>=0.3.0,<0.4.0
langchain-core>=0.3.0,<0.4.0
langchain-community>=0.3.0,<0.4.0

# Document loaders dependencies
pypdf==4.0.1
docx2txt==0.8
python-docx==1.1.0
PyMuPDF==1.25.3
Pillow>=10.2.0

# HTTP client
httpx==0.26.0

# MongoDB
pymongo==4.12.0

# Redis (LLM cache only, not chat memory)
redis==5.0.1

# Utilities
python-dotenv==1.0.0

# Data Analysis dependencies
pandas==2.2.0
numpy==1.26.3
openpyxl==3.1.2  # Excel support
matplotlib==3.8.2  # Chart generation
seaborn==0.13.1  # Enhanced visualizations
tabulate==0.9.0  # Table formatting

# Code execution (Docker SDK)
docker==7.0.0

# JSON schema validation
jsonschema==4.20.0

# BM25 keyword scoring for Hybrid Search (Advanced RAG)
rank-bm25==0.2.2
