
- 
+ # ============================================
+ # Database PostgreSQL Configuration
+ # ============================================
+ # Format: postgresql+psycopg2://username:password@host:port/database
+ DATABASE_URL=postgresql+psycopg2://postgres:postgres@db:5432/jvb_db
+ 
+ # ============================================
+ # Redis Configuration
+ # ============================================
+ # Format: redis://host:port/db
+ REDIS_URL=redis://redis:6379/0
+ REDIS_BLACKLIST_DB=1
+ 
+ # ============================================
+ # JWT Configuration
+ # ============================================

+ SECRET_KEY=my-secret-key

+ 
+ # Thuật toán mã hóa JWT
+ ALGORITHM=HS256
+ 
+ # Thời gian tồn tại Access Token (phút)
+ # Access token dùng cho requests, hết hạn nhanh hơn
+ ACCESS_TOKEN_EXPIRE_MINUTES=15
+ 
+ # Thời gian tồn tại Refresh Token (ngày)
+ # Refresh token dùng để lấy access token mới, hết hạn lâu hơn
+ REFRESH_TOKEN_EXPIRE_DAYS=7
+ 
+ # ============================================
+ # API Configuration
+ # ============================================
+ API_TITLE=JVB API - Java Virtual Bot
+ API_DESCRIPTION=AI-powered Java Virtual Bot API with JWT Authentication
+ API_VERSION=1.0.0
+ 
+ # ============================================
+ # Server Configuration
+ # ============================================
+ # Debug mode (True for development, False for production)
+ DEBUG=True
+ 
+ # Server host (0.0.0.0 để chạy trên tất cả interfaces)
+ HOST=0.0.0.0
+ 
+ # Server port
+ PORT=8000
+ 
+ # ============================================
+ # CORS Configuration
+ # ============================================
+ # Cho phép requests từ những origin này
# Format: origin1,origin2,origin3
CORS_ORIGINS=*
# ============================================
# Admin Configuration
# ============================================
# Danh sách email của admin (format JSON array)
ADMIN_EMAIL=["admin@example.com"]

# ============================================
# MinIO Configuration (Object Storage)
# ============================================
# MinIO endpoint (không có http://)
MINIO_ENDPOINT=minio:9000

# MinIO access credentials
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin

# MinIO bucket name
MINIO_BUCKET_NAME=jvb-documents

# MinIO secure connection (False for local, True for production with HTTPS)
MINIO_SECURE=False

# MinIO public URL (for accessing uploaded files from browser)
MINIO_URL=http://localhost:9000

# ============================================
# Qdrant Configuration (Vector Database)
# ============================================
# Qdrant host
QDRANT_HOST=qdrant

# Qdrant port
QDRANT_PORT=6333

# Qdrant collection name
QDRANT_COLLECTION_NAME=jvb_embeddings

# Qdrant API key (optional, for cloud version)
# QDRANT_API_KEY=your-qdrant-api-key

# ============================================
# Cohere Configuration (Embeddings)
# ============================================
# Cohere API key
COHERE_API_KEY=your-cohere-api-key-here

# Cohere embedding model
COHERE_EMBEDDING_MODEL=embed-multilingual-v3.0

# Vector dimension for Cohere embed-multilingual-v3.0
VECTOR_DIMENSION=1024
